issues/
.trello-retry/

# DX-986 — board-rooted worktrees are regenerable client runtime (CP3) and
# must NEVER be tracked. Embedded worktree gitlinks here would break clones.
boards/*/worktrees/
boards/*/lists.yaml

# Live poller runtime state — rewritten every tick (DX-324 cron tick, DX-558
# sync state, strike/meta in settings.json). Never track.
cron-state.json
boards/*/cron-state.json
settings.json
boards/*/settings.json
sync-root-state.json
